import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ActivatedRoute } from '@angular/router';
import { StudentApiService } from '../../../core/services/student-api.service';
import { ResultDetail, ResultSummary } from '../../../core/models/api.models';
import { formatDate } from '../../../shared/format';

@Component({standalone:true,selector:'app-student-results',imports:[CommonModule],template:`
<div class="page-title"><div><h1>My Results</h1><p>Only your own submissions are visible here.</p></div></div>
<div class="grid grid-2"><div class="card"><div class="table-wrap"><table class="table"><thead><tr><th>Test</th><th>Score</th><th>Submitted</th><th></th></tr></thead><tbody><tr *ngFor="let r of results"><td><strong>{{r.testTitle}}</strong></td><td>{{r.score}} / {{r.totalQuestions}}</td><td>{{date(r.submittedAt)}}</td><td><button class="btn btn-sm" (click)="open(r.resultId)">Open</button></td></tr></tbody></table></div><div *ngIf="!results.length" class="empty"><h3>No results</h3><p>Complete an assigned test to create your first result.</p></div></div>
<div class="card" *ngIf="selected"><div class="page-title" style="margin-bottom:16px"><div><h2 style="font-size:18px;margin:0">{{selected.testTitle}}</h2><p>Submitted {{date(selected.submittedAt)}}</p></div><button class="btn btn-sm" (click)="selected=null">Close</button></div><div class="inline" style="align-items:flex-end;gap:16px"><div><div class="result-score">{{selected.score}}</div><div class="muted">out of {{selected.totalQuestions}}</div></div><span class="badge">{{selected.score/selected.totalQuestions*100 | number:'1.0-0'}}%</span></div><div class="mt"><div class="result-answer" *ngFor="let a of selected.answers;let i=index"><div class="muted">Question {{i+1}}</div><h3 style="font-size:15px">{{a.questionText}}</h3><div class="choice-grid"><div *ngFor="let c of a.choices;let j=index" class="choice"><span>{{letters[j]}}.</span><span style="flex:1">{{c}}</span><span class="answer-chip correct" *ngIf="j===a.correctAnswerIndex">Correct</span><span class="answer-chip wrong" *ngIf="j===a.studentAnswerIndex&&j!==a.correctAnswerIndex">Your answer</span><span class="answer-chip correct" *ngIf="j===a.studentAnswerIndex&&j===a.correctAnswerIndex">Your answer</span></div></div></div></div></div></div>`})
export class StudentResultsComponent{results:ResultSummary[]=[];selected:ResultDetail|null=null;letters=['A','B','C','D'];constructor(private readonly api:StudentApiService,private readonly route:ActivatedRoute){}ngOnInit(){this.api.results().subscribe(r=>{this.results=r;const id=Number(this.route.snapshot.queryParamMap.get('open'));if(id)this.open(id)})}open(id:number){this.api.result(id).subscribe(r=>this.selected=r)}date(v: string){return formatDate(v)}}
